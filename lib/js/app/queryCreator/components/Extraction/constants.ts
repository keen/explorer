import text from './text.json';

export const DEFAULT_LIMIT = 100;
export const HIDE_TIME = 3000;

export const PREVIEW_EVENTS_LIMIT = 100000;

export const HINT_MARKUP = `${text.maximumEventsLimit}<br /><br />${text.emailEventsLimit}`;
